@model IEnumerable<Genre>

<li class="header__nav-item">
    <a class="header__nav-link@(ViewBag.SelectedCategory==null?"active":"")" href=" /Home/List">
        TümFilmler
    </a>

</li>

@foreach (var item in Model)
{
    <li class="header__nav-item">
        <a asp-controller="Home" asp-action="List" asp-route-id="@item.Id"
        class="header__nav-link@(ViewBag.SelectedCategory==item.Id.ToString()?"active":"")" role="button">
            @item.Name 
        </a>
    </li>
}
